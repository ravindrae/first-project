
<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
     <div class="navbar-brand"><%= link_to "Articles",articles_path%></div>
    </div>

    <ul class="nav navbar-nav navbar-right">
      <% if !user_signed_in? %>
      <li class="active"><%= link_to "Sign up",new_user_registration_path%></li>
      <li><%= link_to "Log in",new_user_session_path,alert: "login not successfully...!"%></li>
      <% else %>
      <li class="dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown" href="#"><%=current_user.first_name.upcase%>
        <span class="caret"></span></a>
        <ul class="dropdown-menu">
          <li><%= link_to "Update profile",edit_user_registration_path%></li>
          <li><%= link_to "My articles",user_path(current_user.id)%></li>
        </ul>
      </li>
      <li><%= link_to "Log out",destroy_user_session_path,method: :delete,notice: "Logout successfully...!"%></li>
      <% end %>
    </ul>
  </div>
</nav>
